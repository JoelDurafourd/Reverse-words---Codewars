=begin 
Complete the function that accepts a string parameter, and reverses each word in the string. All spaces in the string should be retained. 
=end 

def reverse_words(str)
  # Go for it
end 


puts assert_equals(reverse_words('The quick brown fox jumps over the lazy dog.'), 'ehT kciuq nworb xof spmuj revo eht yzal .god')
puts assert_equals(reverse_words('apple'), 'elppa')
puts assert_equals(reverse_words('a b c d'), 'a b c d')
puts assert_equals(reverse_words('double  spaced  words'), 'elbuod  decaps  sdrow')
